# brain_qa/plugins/__init__.py
# Plugin package for SIDIX AgentTools extensions.
# Import individual plugins here to auto-register them on startup, e.g.:
#
#   from brain_qa.plugins import example_plugin  # noqa: F401
#
# Each plugin module is responsible for calling ToolRegistry.register()
# inside its own module body so that import is sufficient to activate it.
