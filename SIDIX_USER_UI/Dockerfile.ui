# SIDIX Frontend — Dockerfile
# Build Vite app, serve dengan Nginx

FROM node:18-alpine AS builder

WORKDIR /app

ARG VITE_BRAIN_QA_URL=http://localhost:8765
ENV VITE_BRAIN_QA_URL=$VITE_BRAIN_QA_URL

COPY package*.json ./
RUN npm ci --prefer-offline

COPY . .
RUN npm run build

# ── Runtime: Nginx ─────────────────────────────────────────────
FROM nginx:alpine AS runtime

COPY --from=builder /app/dist /usr/share/nginx/html

# Nginx config untuk SPA (redirect 404 ke index.html)
RUN echo 'server { \
    listen 80; \
    root /usr/share/nginx/html; \
    index index.html; \
    location / { try_files $uri $uri/ /index.html; } \
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2)$ { expires 1y; add_header Cache-Control "public, immutable"; } \
}' > /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
